import { PageHeader } from "@/components/ui";

export default function TutoriaisPage() {
  return (
    <main className="mx-auto max-w-4xl px-6 py-10">
      <PageHeader titulo="Tutoriais" descricao="Em construção — próximo módulo da lista." />
      <p className="text-sm text-neutral-500">Essa tela ainda vai ser implementada.</p>
    </main>
  );
}
